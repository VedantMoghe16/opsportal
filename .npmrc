# CI runs with NODE_ENV=production, which makes `npm ci` omit devDependencies.
# The CI gate (npm run typecheck && npm run lint) needs devDeps — eslint,
# eslint-config-next, @types/* — so force them to install regardless of NODE_ENV.
include=dev
